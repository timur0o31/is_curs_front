const API_CONFIG = {
    BASE_URL: 'http://localhost:8080/api',
    ENDPOINTS: {
        AUTH: '/auth/login',
        STAY_REQUESTS: '/stay-requests',
        FILES: '/files',
    }
};

export default API_CONFIG;